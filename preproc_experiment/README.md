# Preprocessisng experiment

This directory contains `toboggan` files modified to execute *only the pre-processing* components, graph-reduction and k-lowerbound. None of the path decomposition code is executed.
